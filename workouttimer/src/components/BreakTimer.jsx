import React, { useState } from 'react'

function BreakTimer() {
  const [breakTimer, setBreakTimer] = useState(0)

  return (
    <div>
      <h1>Sleepy time blues</h1>
    </div>
  )
}

export default BreakTimer
